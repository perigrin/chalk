# ABOUTME: XS AST node for the C preamble section of an XS file.
# ABOUTME: Emits #define, #include directives and generator comment.
use 5.42.0;
use utf8;
use experimental 'class';

use Chalk::Bootstrap::Target::XS::AST::Node;

class Chalk::Bootstrap::Target::XS::AST::Preamble :isa(Chalk::Bootstrap::Target::XS::AST::Node) {
    method emit() {
        return q{/* Generated by Chalk::Bootstrap compiler — do not edit */
#define PERL_NO_GET_CONTEXT
#include "EXTERN.h"
#include "perl.h"
#include "XSUB.h"

/* NOTE: Direct SVt_PVOBJ construction (newSV_type + ObjectFIELDS) is possible
 * for Symbol and Rule (simple data classes, no ADJUST blocks). Currently using
 * call_method for correctness. See docs/xs-target.md and the outbreed-fulcrumage
 * Binary.xs for a working example of direct ObjectFIELDS manipulation. */

};
    }
}
